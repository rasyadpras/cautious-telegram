spring.application.name=Assessment

spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:assessment_dlabs}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.highlight_sql=true

spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Jakarta
spring.jackson.time-zone=Asia/Jakarta

server.error.include-stacktrace=never

assessment.admin.email=${ADMIN_EMAIL:admin@mail.com}
assessment.admin.password=${ADMIN_PASSWORD:YWRtaW4xMjM=}

assessment.jwt.secret_key=${JWT_KEY:c2VjcmV0d29yZA==}
assessment.jwt.issuer=Developer
assessment.jwt.exp=${JWT_EXP:3600}

springdoc.swagger-ui.path=/docs

spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
